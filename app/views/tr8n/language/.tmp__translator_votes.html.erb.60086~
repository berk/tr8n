<div style="padding:10px; padding-bottom:0px;">
  <%= render(:partial => "translator_header")%>
  
  <div class="section">
    <div class="small_section_title">
      <div style="float:right;font-weight:normal;font-size:10px">
        <%=link_to(trl("view all"), {:controller => "/tr8n/phrases", :action => :view, :translation_key_id => @translation_key.id}) %>
      </div>
      <strong><%=trl("Vote on Translations")%></strong>
    </div>
  
    <div id="tr8n_translator_votes_for_<%=@translation_key.key%>" class="votes_container">
      <%= render :partial => "/tr8n/common/translation_votes", :locals => {:translation_key => @translation_key, :translations => @translations, :section_key => @section_key} %>
    </div>
  
    <div style="padding-top:10px;font-size:11px">
      <%=trl("Don't like any of these?")%> 
			<%=link_to(trl('Translate it yourself.'), {:controller => "/tr8n/phrases", :action => :view, :mode => :add, :translation_key_id => @translation_key.id}) %>
    </div>
  </div>
</div>
