<div id="prohibited_words_container">
  <%=render(:partial => "header", :locals => {:section => "prohibited_words", :label => "Prohibited Words", :description => "", :mode => mode})%>

  <%= form_for(tr8n_current_language, :as => :language, :url => {:controller => :language, :action => :manage}, :html => {:id => 'prohibited_words_form', :method => :post}) do |f| %>
    <div id="prohibited_words_content" class="section grey">
      <% if mode == :edit %>
        <%=f.text_area(:curse_words, :style => "width:100%; height:60px; background:white;")%>
        <div class="tr8n_txtl"  style="color:grey; font-size:10px; padding-top:10px;"><%=tra("Add any prohibited/curse words that should not be allowed in translations for this language. Separate words with commas.")%></div>
      <% else %> 
        <div class="tr8n_txtl" style="width:99%; height:60px; overflow:auto;">
          <%=tr8n_current_language.curse_words%>
        </div>
      <% end %>  
    </div>
  <% end %>  
</div>  