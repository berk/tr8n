<%= render :partial => "/tr8n/admin/common/header" %>
<%= will_filter %>
<br>

<%=link_to_function("+ Add Glossary Term", "tr8nLightbox.show('/tr8n/admin/glossary/lb_update', {height:450})") %>
<br>

<%= render :partial => "/tr8n/admin/common/paginator", :locals => {:objects => @terms} %>

<table class="admin_table">
  <tr>
    <th>
      Keyword
    </th>
    <th>
      Description
    </th>
    <th></th>
  </tr>  
  <% @terms.each_with_index do |term, index| %>
    <tr>
      <td><strong><%=term.keyword%></strong></td>
      <td><%=term.description.gsub("\n", "<br>")%></td>
      <td style="text-align:right; width:100px">
        <%=link_to_function("Update", "tr8nLightbox.show('/tr8n/admin/glossary/lb_update?glossary_id=#{term.id}', {height:450})") %>
        | 
        <%=link_to("Delete", {:action => :delete, :glossary_id => term.id}, {:confirm => "Are you sure you want to delete this glossary term?"}) %>
      </td>
    </tr>
  <% end %>
</table>

<%= render :partial => "/tr8n/admin/common/paginator", :locals => {:objects => @terms} %>
<%= render :partial => "/tr8n/admin/common/footer" %>
