<%= will_filter_scripts_tag %>
<%= will_filter_table_tag(@results, :columns => [
  [:icn, image_tag("tr8n/icons/lightning.png"), :title => "", :style => "width:12px;"],
  :id, :description, 
  [:translator_id, lambda{|lcase|
    if lcase.translator
      link_to(lcase.translator.name, :controller => "/tr8n/admin/translator", :action => :view, :translator_id => lcase.translator.id)
    else
      "System Generated" 
    end  
  }], :created_at
 ]) %>