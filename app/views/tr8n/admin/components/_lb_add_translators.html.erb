<%= tr8n_lightbox_header_tag %>
<div class="inner">
    <div class="hd">
         <%= tr8n_lightbox_title_tag(tra("Add Translators To {comp}", nil, {:comp => @comp.name})) %>
    </div>
    <div class="bd" style="padding-bottom:60px;">
        <%= form_tag({:action => :add_objects, :id => @comp.id}, {:id => 'tr8n_form', :method => :post, :class => "tr8n"}) do %>
        <%= hidden_field_tag(:origin, params[:origin]) %>
        <%= hidden_field_tag :type, params[:type] %>
        <%= hidden_field_tag :comp_id, params[:comp_id] %>
        <div style="max-height:340px;overflow:auto;font-size:12px;">
            <% @translators.each do |app_translator| %>
                <div style="padding:5px; border-bottom:1px dotted #eee">
                    <%= check_box_tag("ids[]", app_translator.translator.id) %> &nbsp; <%= app_translator.translator.name %>
                </div>
            <% end %>
        </div>    
        <% end %>
    </div>
    <div class="ft">
       <%= tr8n_lightbox_buttons_tag %>
    </div>
</div>
<%= tr8n_lightbox_footer_tag %>
